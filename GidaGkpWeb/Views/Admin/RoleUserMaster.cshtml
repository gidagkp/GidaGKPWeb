@using GidaGkpWeb.Infrastructure.Authentication;
@using GidaGkpWeb.Models;
@using DataLayer;

@{
    ViewBag.Title = "Role User Master";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";

    var ApplicantData = ViewData["PermissionType"] as List<LookupModel>;
}
<script src="~/Scripts/App/Masters/RoleMaster.js"></script>
<link rel="stylesheet" href="~/styles/table_form.css">
<script src="~/Scripts/App/Masters/navigation.js"></script>
<div class="container">
    <div class="col-sm-12 col-xs-12" style="padding:10px 0 10px 0; margin-bottom: 50px; border-bottom:1px solid rgb(170, 170, 170);">
        <h3 style="font-size:18px; padding-top: 13px;"> Role Wise Permission </h3>
    </div>
    <div class="col-sm-12 col-xs-12" style="padding: 0px;">
        <div class="row">
            <div class="form-group col-sm-3 col-xs-12">
                <label for="user" class=""> Select Department  </label>
                <select id="ddlDepartment" name="Department">
                </select>
            </div>
            <div class="form-group col-sm-3 col-xs-12">
                <label for="user" class=""> Select Designation  </label>
                <select id="ddlDesignation" name="Designation">
                </select>
            </div>
            <div class="form-group col-sm-3 col-xs-12">
                <label for="user" class=""> Select Role  </label>
                <select id="ddlRole" name="ddlRole">
                </select>
            </div>
            @*<div class="form-group col-sm-3 col-xs-12">
            <label for="user" class=""> Select User  </label>
            <select id="ddlGidaUser" name="ddlGidaUser">
            </select>
        </div>*@
            @*<div class="form-group col-sm-9 col-xs-12">
            <table class="table table_css_main_cl table-bordered">
                <thead>
                    <tr>
                        <th class="text-center">Username</th>
                        <th class="text-center">Name</th>
                        <th class="text-center">E Mail ID </th>
                        <th class="text-center">Phone umber</th>
                        <th class="text-center">Department</th>
                        <th class="text-center">Designation</th>
                    </tr>
                </thead>
                <tbody id="roleUserTBody">
                </tbody>
            </table>
        </div>*@
        </div>

        <div class="table-responsive" style="padding:10px 0 0px 0;">
            <table class="table table_css_main_cl table-bordered">
                <thead id="rolePermissionTHead">
                    <tr>
                        <th class="text-center">#</th>
                        <th class="text-center">Page ID</th>
                        <th class="text-center">Page Name</th>
                        @if (ApplicantData != null && ApplicantData.Count > 0)
                        {
                            foreach (var data in ApplicantData)
                            {
                                <th class="text-center" data-permission-id="@data.LookupId">@data.LookupName</th>
                            }
                        }
                    </tr>
                </thead>
                <tbody id="rolePermissionTBody">
                </tbody>
                @*<tr>
                        <td class="text-center">4</td>
                        <td class="text-center">NoticeLists</td>
                        <td class="text-center"><select><option>View</option> <option>Edit</option> <option>Hide</option></select></td>
                    </tr>*@
            </table>
        </div>

        <div class="col-sm-12 col-xs-12" style="padding:0px 0 20px 0;">
            <button type="button" class="pull-right addCommittee_btn" id="btnSaveUserPermission">Sumbit</button>
        </div>

    </div>



</div>

